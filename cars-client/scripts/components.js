function carCard(car){
    return `<div class="car-card">
                <div>
                    <h4> ${car.name}</h4>
                    <h4> ${car.model} | ${car.year} </h4>
                </div>
            </div>`;
}

function userCard(user){

}